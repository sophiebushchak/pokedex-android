package com.example.pokedata.rest

/**
 * Configuration file for variables that may change when connecting to the back end.
 */
class PokeDataApiConfig {
    companion object {
        const val HOST = "http://127.0.0.1:8080/"
        const val BASE_URL = "api/"
    }
}